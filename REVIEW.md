# 저장소 개선·교차 검토 기록

기록 범위: 전체 개편 커밋 `7705db5`. 이후 문서 이동은 링크에만 반영한다.

검토일: 2026-09-05. 대상은 이 저장소의 가이드·실행 프롬프트·문서 진입점이다. 목표는 **조각 간 정보 은닉, 높은 응집도, 작은 AI 작업 맥락**이며, 실제 사용자 프로젝트의 성능·토큰 사용량 개선 실험과는 구분한다.

## 1. 검토 방식

루트 통합 담당과 세 전문 에이전트가 병렬로 조사한 뒤, 개선안을 서로 비판하고 수정된 문서를 다시 검토했다.

| 담당 | 주된 작성 범위 | 교차 검토 |
| --- | --- | --- |
| 통합 | 공통 모듈 기준, README, PRD, README 작성, 설치 안내, 유지보수 규칙 | 세 담당의 개선안과 실제 diff, 문서·설치 정합성 |
| architecture | Method-R, O-W, 설계 프레임워크, 코드·폴더 구조 | 공통 계약의 동시성·이벤트 실패 반례, 타 담당과 규칙 정합성 |
| diagrams | 4종 DSL, 패턴 예제, 시스템 흐름, tools.camp 문법 | Method-R·프롬프트·PRD의 표기와 의미 일치 |
| prompts | 실행 프롬프트 12개 | 공통 맥락 양식, 종료 조건, 조율·소유권 원칙의 실제 적용 |

각 담당에게 쓰기 파일 소유권을 나눴다. 공유 원칙은 먼저 전달하고 서로의 파일을 덮어쓰지 않았다. 이번 실행은 환경에서 상속한 모델을 사용했으며 별도 모델 등급 변경이나 비용 측정을 수행하지 않았다. 기존 사용자 편집의 **역할별 모델 배정·비용 절감 의도**는 프롬프트와 README에 유지했다.

## 2. 비판·개선 반복

### 1차 — 독립 조사와 개선안 비판

| 발견 | 문제가 되는 이유 | 결정 |
| --- | --- | --- |
| 파일·내부 이벤트·계층의 강제 분리 | 구현은 작아져도 찾아갈 계약·연결·문서는 늘 수 있음 | 변경 이유·불변조건·계약으로 분리하고 중단·병합 기준 추가 |
| Orchestration=동기, Choreography=비동기 | 전송 방식이 제어·복구 소유자를 설명하지 않음 | 책임·조율·프로세스·전송을 별도 관점으로 구분 |
| 전역 Singleton 권장과 명시적 의존 원칙 충돌 | 상태와 수명·쓰기 책임이 숨음 | 조립 위치·주입·공유 수명·쓰기 소유권 명시 |
| 모든 PRD·화면·모듈에 같은 산출물 강제 | 관련 없는 내용을 생성하고 중복 갱신을 요구 | 필요한 관점·독립 책임만 상세화, 원본 링크 사용 |
| 테스트·로그·UX 조사 범위의 끝없는 확장 | 목표와 무관한 변경, 비용 증가, 종료 불명확 | 위험과 성공 기준으로 범위를 정하고 유한 재검토 |
| DSL·설명·렌더러 구현의 혼동 | 의미가 바뀌거나 미검증 출력을 성공으로 보고 | 독자 DSL 보존, 별도 관점 보완, 검증 수준 구분 |

통합 검토는 이벤트 강제 완화가 사용자의 조율자 취향까지 없애지 않도록 요구했다. 이에 따라 **형제 조각의 결과 연결은 상위가 맡는 기본값**을 유지하면서 내부 순수 함수·좁은 Port 호출은 허용했다.

### 2차 — 변경 파일을 이용한 반례 검토

| 지적 | 수정과 확인 |
| --- | --- |
| 취소 판정·저장만 원자화하면 출고와 환불의 경합을 놓침 | 공통 가이드·Method-R에 취소 진행 중 경쟁 명령의 허용 조건과 동시 요청 검증 기준 추가 |
| 저장 후 이벤트 발행 전 종료되면 사실이 전달되지 않음 | 저장·발행 사이 실패의 복구·재발행 소유자 명시; Outbox 일괄 도입은 요구하지 않음 |
| navigation 설명이 내부 API·큐까지 포괄함 | 화면 이동과 그 판단에 필요한 API·처리로 제한하고 내부 협력은 jobflow/계약으로 분리 |
| DSL 유지 정책과 Mermaid 대체 허용이 충돌함 | DSL을 원본으로 유지하고 시간순 통신·다른 상태 관점만 별도 보완 |
| PRD 반환 분기 예제에서 호출자의 최종 결과가 불분명함 | Complete/Reject가 Run의 성공·거절 결과를 구성함을 설명 |
| 단일 응답 콜백을 일반 이벤트라고 다시 설명함 | 새 예제는 RequestData로 명명하고 OnNeedData는 레거시 요청 콜백으로 설명 |
| 재시도 횟수·리뷰 점수로 완료를 대체할 수 있음 | 필수 결함·필수 검증 차단이 남으면 미완료, 추가 회차에는 새 근거 요구 |
| 사이트 프롬프트에 V/H/> 의미가 다시 혼입됨 | 로컬 layout 파서의 V 세로·> 가로·H 미지원 확인 후 전용 가이드 참조로 교정 |
| 설치 안내가 실제 제거 범위를 좁게 설명함 | 매니페스트와 현재 프롬프트 스킬명 디렉터리를 제거하는 실제 동작으로 정정 |

수정 후 해당 위치와 영향을 받는 문서를 다시 대조했다. 문체 선호와 의미·계약·검증 결함은 구분했다.

## 3. 주요 결과물

- [모듈 경계와 최소 맥락 가이드](guides/module-boundary-guide.md): 계약 양식, 쓰기·실패 소유권, 맥락 패킷, 탐색 확대 조건, 변경 검증과 평가 기준.
- [README](README.md): 프롬프트 하나에서 시작하는 진입점, 모든 가이드·프롬프트의 목적과 추가 탐색 조건.
- [Method-R](guides/method-R.md), [O-W](guides/orchestrator-worker-pattern-guide.md): 독자 분할·조율 취향을 유지하면서 과분해와 숨은 결합을 줄이는 적용 조건.
- [PRD](guides/prd-writing-guide.md): 7 Part·점진적 상세화·다이어그램/객체/이벤트/시나리오 관점을 유지하고 계약·요구사항·검증을 연결.
- [멀티 에이전트 프롬프트](prompts/multi-agent-task-prompt.md): 파일 소유권, 실제 모델 지원에 따른 배정, 독립 반례 검토, 명확한 완료/미완료 판정.
- [테스트](prompts/comprehensive-test-prompt.md)·[로깅](guides/detailed-logging-guide.md): 경계·위험 중심 검증, 필요한 계측, 실제 출력과 부작용 확인.
- [AGENTS.md](AGENTS.md): 이후 편집에서 사용자 변경·독자 문법·원본 일관성과 검증 범위를 유지하는 규칙.

### 분량 변화

측정 범위는 README·설치 안내·guides·prompts이며 유지보수 규칙과 이 검토 기록은 제외한다. 시작 시 사용자 미커밋 변경이 포함된 작업 트리를 기준으로 비교했다.

| 문서 | 수정 전 | 수정 후 |
| --- | ---: | ---: |
| 실행 프롬프트 12개 합계 | 3,950줄 | 942줄 |
| PRD 작성 가이드 | 1,533줄 | 226줄 |
| README | 281줄 | 104줄 |
| 테스트 프롬프트 | 1,034줄 | 104줄 |
| 상세 로깅 프롬프트 | 1,104줄 | 98줄 |

줄 수는 중복과 읽기 부담을 줄인 보조 지표다. 계약·예외·검증 근거의 보존 여부는 별도로 검토했다.

## 4. 검증 결과

- Markdown 정적 검사: 31개 문서의 상대 링크·앵커 200개와 펜스 균형을 확인해 통과했다. README가 가이드/프롬프트 27개를 모두 안내하고, 프롬프트 12개가 공통 경계 가이드를 참조함을 확인했다. 객체 목록이 있는 완결된 jobflow 블록 23개의 연결 대상 선언도 대조했다. 이 검사는 문서 의미 검토·실제 렌더링을 대신하지 않는다.
- `git diff --check`: 최종 통합 파일에서 통과했다.
- 설치 통합 확인: 임시 `CLAUDE_SKILLS_DIR`에서 설치→업데이트→제거 통과. 가이드/프롬프트 27개와 README 복사, 스킬 13개 생성 및 매니페스트·원본 일치를 확인했다. 실제 사용자 홈은 변경하지 않았다.
- C# 예제: .NET SDK 9.0.305, `net9.0` 임시 프로젝트에서 문서의 `csharp` 블록을 추출하여 `dotnet run --project <임시 경로>/Example.csproj --verbosity quiet`로 컴파일·실행했다. 빈 입력 무쓰기, 정상 결과 1회 저장, 저장 불가, 예상 밖 오류 전파, 사전/저장 중 취소, 취소 토큰 전달, 저장 완료 전 성공 방지를 포함한 9개 검증을 통과했다. 실제 DB adapter는 없으므로 DB 원자성·오류 변환의 통합 검증은 수행하지 않았다.
- 독자 DSL: 로컬 `tools.camp`의 깨끗한 스냅샷 `d12f23014e6fed86db8012f007637a0709d61eaf`, Node v22.19.0으로 확인했다. `src/lib`의 관련 소스와 기존 테스트를 임시 경로에 복사하여 result-line 69개·XSS 32개 assertion 통과, 다이어그램 담당 가이드의 구체 DSL 예시 48개(jobflow 30, navigation 12, layout 3, state 3) 변환·SVG/HTML 문자열 생성 smoke 통과. 브라우저 화면·모든 예제의 의미·다른 제품 버전까지 검증한 것은 아니다.

추가로 확인한 구현 차이: 위 tools.camp 스냅샷은 `orchestrator:`/`scope:`의 제어 의미를 해석하지 않고 `master:`도 SVG에 반영하지 않는다. `jobflow`의 ` : 라벨`은 별도 전이 라벨로 처리하지 않는다. 이는 기존 문서의 의미와 구현 지원 사이 차이이며, 버전 범위와 작성 방법을 문법 가이드에 명시했다. 이 작업에서 외부 렌더러 코드는 수정하지 않았다.

최종 독립 평가는 사용자 지시 우선, 설계 작업 범위, DSL 보존, 필요한 맥락 확대, 완료/미완료, 모델 선택 불가 시 처리를 다시 대조했다. 커밋을 막을 필수 모순은 발견하지 못했고, 선택적 지적인 “설계 작업에서 Developer가 문서도 다룬다”는 표현도 반영했다.

## 5. 평가 범위와 남은 한계

| 평가축 | 문서에서 확인한 변화 | 실제 프로젝트에서 추가 확인할 것 |
| --- | --- | --- |
| 응집도 | 변경 이유·불변조건·분할 중단/병합 기준 | 대표 기능 변경의 동시 수정 범위 |
| 결합도 | 공개 경계·Port·조립·소비자 탐색 명시 | 실제 import·DB 접근·동적 구독 위반 |
| 상태 소유권 | 쓰기·경쟁 명령·실패·복구의 담당 정의 | 실제 저장소·외부 서비스에서의 계약 보장 |
| 맥락 효율 | 필요한 절과 원본 위치를 담는 작업 패킷 | 동일 작업의 읽은 파일·토큰·시간 전후 비교 |
| 검증 가능성 | 요구사항·반례·계약·실행 결과 연결 | 프로젝트별 계약·통합·사용자 경로 테스트 |
| 구조 비용 | 강제 문서·계층·이벤트·반복 조사 축소 | 실제 변경 시 새 연결과 운영 비용 |

문서가 줄었다는 사실은 토큰 절감 효과의 실증 결과가 아니다. 이 검토는 문서 규칙·예제·전달 경로를 개선한 것이며 모든 프로젝트에 최적 구조를 보장하지 않는다. 사용자 프로젝트에 적용할 때 계약과 필요한 맥락을 다시 확인해야 한다.
